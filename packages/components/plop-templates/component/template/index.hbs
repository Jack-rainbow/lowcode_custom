
{{name}}.install = (app) => {
  app.component({{name}}.name, {{name}})
}

export default {{name}}


import {{name}} from './{{name}}.{{type}}'

import {{name}}meta from './{{name}}.define.meta.js'

const {{name}}Info = {
  component: {{name}},
  meta: {{name}}meta
}

export default {{name}}Info
